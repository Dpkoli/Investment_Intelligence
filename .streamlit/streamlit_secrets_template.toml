# ── Contrarian Radar — Streamlit Cloud Secrets Template ───────────────────────
#
# HOW TO USE
# ──────────────────────────────────────────────────────────────────────────────
# 1. Go to share.streamlit.io → your app → ⋮ → Settings → Secrets
# 2. Paste the block below, replacing placeholder values with real credentials.
# 3. Click Save — the app restarts and reads these as both st.secrets["KEY"]
#    and os.environ["KEY"] automatically (no code changes required).
#
# DO NOT commit real credentials. This file contains only placeholders.
# Add '.streamlit/secrets.toml' to .gitignore (already done).
# ──────────────────────────────────────────────────────────────────────────────

SUPABASE_URL             = "https://axvpzbdlfbuighvaussx.supabase.co"
SUPABASE_ANON_KEY        = "your_anon_key_here"
SUPABASE_SERVICE_ROLE_KEY = "your_service_role_key_here"
