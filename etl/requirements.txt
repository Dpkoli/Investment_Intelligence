# Contrarian Radar ETL — Python dependencies
# Tested with Python 3.11+

# HTTP + retry
requests>=2.31.0
urllib3>=2.0.0

# Data validation
pydantic>=2.5.0

# HTML scraping (Cassandra pipeline)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Environment management (dev only)
python-dotenv>=1.0.0
